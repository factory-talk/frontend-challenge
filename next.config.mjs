/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['openweathermap.org'], // Add the domain(s) where your images are hosted
      },
};

export default nextConfig;
